apiVersion: v1
kind: Service
metadata:
  name: {{ include "waf3.fullname" . }}
  labels:
    {{- include "waf3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "waf3.selectorLabels" . | nindent 4 }}
